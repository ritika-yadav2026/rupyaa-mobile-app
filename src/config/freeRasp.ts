/**
 * freeRASP static config — not env-driven.
 * watcherMail must match the email used at https://my.talsec.app/
 */
export const freeRaspStaticConfig = {
  watcherMail: 'piyush.beli@wecredit.co.in',
  iosAppTeamId: '',
} as const;
