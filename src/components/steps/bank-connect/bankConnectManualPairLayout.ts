import { spacing } from '@/src/theme';

/**
 * Shared min-height for the primary AA button and the secondary ActionCard when both show
 * on Bank Connect mobile (matches ~ title + 2-line subtext + card padding).
 */
export const BANK_CONNECT_MANUAL_PAIR_MIN_HEIGHT = spacing.base * 5;
